# CUDA machine notes

Copy the facts below for **each** machine used. Do not replace old runs when a
driver or toolkit changes; date a new entry so benchmark results remain
reproducible.

## Run metadata

- Date/time and timezone:
- Git commit / working-tree note:
- Machine name: `GTX 1080 / RTX 5060 Ti / other`
- OS and version:
- CPU and RAM:
- GPU exact model and VRAM:
- GPU compute capability (source or `deviceQuery` output):
- Display attached to benchmark GPU? `yes/no`
- NVIDIA driver (`nvidia-smi`):
- CUDA compiler (`nvcc --version`):
- CUDA runtime reported by executable:
- Host compiler:
- CMake and generator:
- `CMAKE_BUILD_TYPE` or `--config`:
- `CMAKE_CUDA_ARCHITECTURES`:
- Nsight Compute / Systems versions:
- Power mode, temperature, and background workload notes:

## Exact commands

```text
# Configure:

# Build:

# Run:
```

## Observations

- Correctness cases run:
- First surprising result:
- Hypothesis to test next:
- Anything that makes this run non-comparable:

