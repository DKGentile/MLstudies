# Systems evidence

Use one row in `experiment_log.csv` per run. Change one independent variable at
a time and preserve enough environment detail to reproduce the observation.

Complete `LAB_REPORT.md` each Friday. If something crashes or hangs, use
`INCIDENT_NOTE.md` before "fixing until it works". A named causal chain is more
valuable than a screenshot of green tests.

