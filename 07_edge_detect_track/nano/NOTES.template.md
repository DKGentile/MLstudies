# Jetson Nano Environment Notes

**Captured:**  
**Git commit:**  
**Board / memory:**  
**Power mode / clocks:**  
**Cooling:**  

```text
uname -a:
cat /etc/nv_tegra_release:
python --version:
nvcc --version:
TensorRT version:
OpenCV version:
free -h:
df -h:
```

**Known-working engine build command:**

**Known-working inference command:**

**Image backup location and date:**

